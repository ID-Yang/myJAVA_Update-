import java.util.*;
public static void main(String[] args){
    character[] ray={'a','b','c','d'};
    
   List< character> l= Arrays.asList(ray)
sist< character> l= Arrays.asList(ray);
    List< character> l= Arrays.asList(ray);
    List< character> l= Arrays.asList(ray);
}